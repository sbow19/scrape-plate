/* Stateful container of popup container component */
import React from 'react';
import MainPopupView from './main';

const MainPopupViewContainer = () => {
	return (
		<>
			<MainPopupView />
		</>
	);
};

export default MainPopupViewContainer;
