import React from 'react';
import * as styles from '#styles/popup.module.css';
; // For custom spinner styles

const Spinner = () => {
  return (
  <div className={styles.spinnerContainer}>
  <div className={styles.spinner}>

  </div>
  </div>)
};

export default Spinner;